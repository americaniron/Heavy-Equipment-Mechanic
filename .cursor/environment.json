{
  "name": "FixMyIron (Heavy-Equipment-Mechanic)",
  "install": "npm install && npx wrangler d1 migrations apply fixmyiron-staging --local --config workers/api/wrangler.toml --persist-to .wrangler-local",
  "terminals": [
    {
      "name": "api",
      "command": "npx wrangler dev --config workers/api/wrangler.toml --persist-to .wrangler-local --port 8787 --ip 127.0.0.1"
    },
    {
      "name": "spa",
      "command": "cd client && npx vite --host 127.0.0.1 --port 5173"
    }
  ],
  "ports": [5173, 8787]
}
